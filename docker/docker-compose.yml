include:
  - ./docker-compose-base.yml

# To ensure that the container processes the locally modified `service_conf.yaml.template` instead of the one included in its image, you need to mount the local `service_conf.yaml.template` to the container.
services:
  ragflow:
    depends_on:
      mysql:
        condition: service_healthy
    image: ${RAGFLOW_IMAGE}
    # Example configuration to set up an MCP server:
    # command:
    #   - --enable-mcpserver
    #   - --mcp-host=0.0.0.0
    #   - --mcp-port=9382
    #   - --mcp-base-url=http://127.0.0.1:9380
    #   - --mcp-script-path=/ragflow/mcp/server/server.py
    #   - --mcp-mode=self-host
    #   - --mcp-host-api-key=ragflow-xxxxxxx
    container_name: ragflow-server
    ports:
      - ${SVR_HTTP_PORT}:9380
      - 80:80
      - 443:443
      - 5678:5678
      - 5679:5679
      - 9382:9382 # entry for MCP (host_port:docker_port). The docker_port must match the value you set for `mcp-port` above.
    volumes:
      - ./entrypoint.sh:/ragflow/entrypoint.sh
      - ./ragflow-logs:/ragflow/logs
      - ./nginx/ragflow.conf:/etc/nginx/conf.d/ragflow.conf
      - ./nginx/proxy.conf:/etc/nginx/proxy.conf
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf
      - ../history_data_agent:/ragflow/history_data_agent
      - ./service_conf.yaml.template:/ragflow/conf/service_conf.yaml.template
      - ../api/apps/sdk/doc.py:/ragflow/api/apps/sdk/doc.py
      - ../api/db/services/file_service.py:/ragflow/api/db/services/file_service.py
      - ../rag/utils/minio_conn.py:/ragflow/rag/utils/minio_conn.py
      - ../rag/svr/task_executor.py:/ragflow/rag/svr/task_executor.py
      - ../deepdoc/parser/monkeyocr_parser.py:/ragflow/deepdoc/parser/monkeyocr_parser.py
      - ../deepdoc/parser/__init__.py:/ragflow/deepdoc/parser/__init__.py
      - ../rag/app/naive.py:/ragflow/rag/app/naive.py
      - ../rag/app/presentation.py:/ragflow/rag/app/presentation.py
      - ../rag/nlp/__init__.py:/ragflow/rag/nlp/__init__.py
      - ../api/db/services/document_service.py:/ragflow/api/db/services/document_service.py
      - ../api/db/services/document_content_service.py:/ragflow/api/db/services/document_content_service.py
      - ../api/db/db_models.py:/ragflow/api/db/db_models.py
      - ../api/db/services/__init__.py:/ragflow/api/db/services/__init__.py
      - ../api/db/services/task_service.py:/ragflow/api/db/services/task_service.py
      - ../api/db/services/conversation_service.py:/ragflow/api/db/services/conversation_service.py
      - ../api/db/services/canvas_service.py:/ragflow/api/db/services/canvas_service.py
      - ../api/apps/api_app.py:/ragflow/api/apps/api_app.py
      - ../api/apps/conversation_app.py:/ragflow/api/apps/conversation_app.py
      - ../api/apps/sdk/session.py:/ragflow/api/apps/sdk/session.py
    env_file: .env
    environment:
      - TZ=${TIMEZONE}
      - HF_ENDPOINT=${HF_ENDPOINT}
      - MACOS=${MACOS}
      - MONKEYOCR_URL=${MONKEYOCR_URL:-https://u727268-a9a5-bda5fc5a.nmb1.seetacloud.com:8443/monkeyocr}
      - MONKEYOCR_TIMEOUT=${MONKEYOCR_TIMEOUT:-300}
    networks:
      - ragflow
    restart: on-failure
    # https://docs.docker.com/engine/daemon/prometheus/#create-a-prometheus-configuration
    # If you use Docker Desktop, the --add-host flag is optional. This flag ensures that the host's internal IP is exposed to the Prometheus container.
    extra_hosts:
      - "host.docker.internal:host-gateway"
  # executor:
  #   depends_on:
  #     mysql:
  #       condition: service_healthy
  #   image: ${RAGFLOW_IMAGE}
  #   container_name: ragflow-executor
  #   volumes:
  #     - ./ragflow-logs:/ragflow/logs
  #     - ./nginx/ragflow.conf:/etc/nginx/conf.d/ragflow.conf
  #   env_file: .env
  #   environment:
  #     - TZ=${TIMEZONE}
  #     - HF_ENDPOINT=${HF_ENDPOINT}
  #     - MACOS=${MACOS}
  #   entrypoint: "/ragflow/entrypoint_task_executor.sh 1 3"
  #   networks:
  #     - ragflow
  #   restart: on-failure
  #   # https://docs.docker.com/engine/daemon/prometheus/#create-a-prometheus-configuration
  #   # If you're using Docker Desktop, the --add-host flag is optional. This flag makes sure that the host's internal IP gets exposed to the Prometheus container.
  #   extra_hosts:
  #     - "host.docker.internal:host-gateway"
